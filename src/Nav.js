export default class Nav {
    constructor ($target) {
        this.$target = $target
        this.render()
    }

    render = () => {
        // 렌더 함수에는 렌더링 되어지는 부분만 넣어야하는걸까?
        // 아니면 그냥 렌더링 하는것들도 다 넣을까

    }
}